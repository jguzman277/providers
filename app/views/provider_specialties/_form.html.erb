<%= form_with(model: provider_specialty, local: true) do |form| %>
  <% if provider_specialty.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(provider_specialty.errors.count, "error") %> prohibited this provider_specialty from being saved:</h2>

      <ul>
      <% provider_specialty.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="form-group">
    <%= form.label :provider_specialty %>
    <%= form.text_field :provider_specialty, :class => "form-control" %>
  </div>

  <div class="form-group">
    <%= form.label :active %>
    <%= form.check_box :active, :class => "form-control" %>
  </div>

  <div class="form-group">
    <%= form.submit "Submit", :class => "form-control btn btn-dark" %>
  </div>
<% end %>
